### 连个单位都没法改？干脆推翻重写！服务器监控终极形态长这样

事情是这样，我不是用淘汰的笔记本自己搭了一个家庭服务器，用来高些影音、iptv、跑个青龙脚本啥的。有服务在运行就肯定需要监控呀，然后就装了很多市面上常见的监控工具，比如1panel beszel、homepage。然后我就发现好像都没法完全满足的要求，我想的是在一个界面能展示我的家庭服务器部署的东西docker、定时任务、其他的一些应用，然后要展示系统当前的占用cpu、内存、硬盘、网络，然后个服务的端口以及运行状态。

homepage可以满足很多，但是它的系统性能监控只能展示GiB这种格式、看着就很难受，不喜欢，想改。但是发现好像改不了，然后就打算另起炉灶，啥都满足不了我自己那我自己搞一个不就是了（突然想起来我也是个程序员啊）。

要求很简单上面已经说了，还有很重要的一点是必须要轻量，不然我的二手淘汰4210u+8gddr3l是真的没法承载。

说干就干，首先是技术选型，既然要轻量那必须得是go啊，前端的话就react组件化好维护，毕竟还是得带点交互的，要不然就纯html+css更轻量。

然后就是数据刷新，那必然得是轮训 简单粗暴但是可靠，我这小本本的弱鸡性能把轮训间隔拉长一点就可以认为没有损耗。

认证&鉴权  这是啥 没听说过，就我自己用这玩意纯纯浪费性能。

部署的话必然得是docker啊，简单轻松 哪里都能跑 比java还吊

然后就是页面主题，虽然是性能羸弱但是也不能没电追求啊，听说最近液态玻璃比较火，那咱也整（GPU顶不住再说）



确定了这些，大体的功能就罗列了差不多了，然后最后一步丢给claudecode  静等结果。









有需要的留言哈  点关注发送监控 也可以获得部署命令



质谱：

事情是这样，前阵子我用一台淘汰的旧笔记本，搭了个家庭服务器。平时拿来搞搞影音、IPTV、挂个青龙脚本，折腾得不亦乐乎。

有服务在跑，监控肯定少不了。市面常见的工具我装了个遍：1panel、Beszel、Homepage……但发现始终没法完全满足我的强迫症。

我的需求其实很简单：**在一个界面搞定所有！** 既要展示部署的Docker容器、定时任务、应用端口及运行状态，又要监控当前系统的CPU、内存、硬盘、网络实时占用。

比如Homepage，功能是很强大，但它的系统性能监控竟然只能展示GiB这种格式！看着就极其难受。想改？发现根本无从下手。

与其妥协，不如另起炉灶。既然现有的工具都满足不了我，那我自己搞一个不就是了？（突然想起来，咱不也是个程序员嘛！）

**我的要求很简单，但也极其严苛：**

- **绝对轻量**：我的二手淘汰神机（4210U + 8G DDR3L）是真的扛不住吃资源的猛兽。
- **技术选型**：后端必须Go，天生高并发低内存；前端React，组件化好维护，毕竟还得带点交互。
- **数据刷新**：简单粗暴的轮训。把间隔拉长一点，弱鸡性能几乎零损耗，主打一个简单可靠。
- **认证&鉴权**：这是啥？没听说过。就我自己用，加认证纯属浪费性能，直接砍掉！
- **部署方式**：必须Docker，一键部署，哪里都能跑，比Java还香。
- **颜值追求**：性能虽弱，审美不能降。听说最近“液态玻璃”风格很火？那咱也整上！（GPU要是顶不住再说）。

大体的功能罗列完毕，最后一步：需求打包，直接丢给Claude Code，静等奇迹发生。

现在，它来了！轻量、极客、还要有颜值。

有需要部署到自己家庭服务器的小伙伴，**点赞+关注，后台发送暗号【监控】，立刻获取一键Docker部署命令！**





deepseek：

### Homepage 什么都好，就这点我忍不了，于是自己写了一个

事情是这样的——我拿淘汰的旧笔记本搭了个家庭服务器，4210U + 8G DDR3L，主打一个废物利用。平时跑跑影音库、IPTV、青龙脚本啥的，倒也挺香。

但有个问题：服务一多，就得盯着点状态吧。于是我装了一圈市面上常见的监控面板：1Panel、Beszel、Homepage……怎么说呢，各有各的好，但就是没有完全称手的。

Homepage 其实最接近我的理想——能展示 Docker 容器状态、定时任务、应用入口，也支持系统性能监控。但那个性能监控的数值只支持 GiB 单位，看着贼难受。我翻了一圈文档和配置，发现没法改。

那一刻我突然反应过来：**我也是个程序员啊，凭什么忍？**

自己搞一个不就完了。

------

需求其实很清楚：

- 一个页面展示所有服务入口、端口、运行状态
- Docker 容器状态
- 定时任务（Cron）列表
- 实时系统占用：CPU、内存、硬盘、网络
- 必须够轻量，不能把我的老伙计 4210U 压垮

目标明确，开搞。

------

**技术选型**

既要轻量又要能带点交互，后端自然选了 Go，编译完就一个二进制，省内存。前端本来想纯 HTML + CSS，但考虑到要维护组件状态，还是上了 React，组件化好折腾。

数据刷新嘛，没上 WebSocket，直接轮询——简单、粗暴、可靠。把间隔拉长点，对我这弱鸡性能来说基本感觉不到损耗。

认证鉴权？不存在的。就我自己用，加这层纯属浪费算力。

部署必须 Docker 一条龙，镜像一打，docker run 走起，哪里都能跑，比 Java 还方便（Java 党别打我）。

------

页面风格也不能太寒碜。性能可以差，但审美不能丢。最近液态玻璃（Glassmorphism）不是挺火嘛，安排上。要是 GPU 顶不住再换，反正先好看再说。

功能清单列完，最后一步——**全部丢给 Claude Code，静等结果。**

你猜怎么着？它真给我写出来了。

------

目前已经在我的小破本上跑起来了，资源占用感人，界面看着也顺眼。后面打算把配置和部署方式整理一下，发出来分享给有同样需求的朋友。

**有需要的留言哈，点关注发送“监控”也可以获取部署命令～**

